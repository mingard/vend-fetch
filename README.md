### Startup

```javascript

// New instance

let vend = new VendFetch({
  domain_prefix: '{domain_prefix}',
  client_id: '{client_id}',
  client_secret: '{client_id}',
  refresh_token: '{refresh_token}'})

```